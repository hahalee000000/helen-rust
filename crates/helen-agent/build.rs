//! Build script: no-op (frontend and agent files are committed to the repo)
//!
//! Both frontend/ and agent/ directories are part of the Rust source tree,
//! making the build fully self-contained.

fn main() {
    // Intentionally empty — assets are committed to the repo.
}
