[package]
name = "helen-rust"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Helen language Rust reimplementation — installable CLI package (binary: helen)"

[[bin]]
name = "helen"
path = "src/main.rs"

[dependencies]
helen-core = { version = "0.1.0", path = "../helen-core" }
helen-parser = { version = "0.1.0", path = "../helen-parser" }
helen-semantic = { version = "0.1.0", path = "../helen-semantic" }
helen-interpreter = { version = "0.1.0", path = "../helen-interpreter" }
helen-runtime = { version = "0.1.0", path = "../helen-runtime" }
helen-lsp = { version = "0.1.0", path = "../helen-lsp" }
helen-ffi = { version = "0.1.0", path = "../helen-ffi", optional = true }
serde_json.workspace = true
chrono.workspace = true

[features]
default = []
python-ffi = ["helen-ffi/python-ffi"]
