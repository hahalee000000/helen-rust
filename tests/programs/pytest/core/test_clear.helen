import std.core.*
import std.str.*
import std.list.*
import std.dict.*
import std.math.*
import std.debug.*

        main {
            let list = [1, 2, 3]
            list.clear()
            print(list)
        }
        