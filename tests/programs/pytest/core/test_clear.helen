import std.core.*
import std.str.*
import std.list.*
import std.dict.*
import std.math.*
import std.debug.*

        main {
            let my_list = [1, 2, 3]
            my_list.clear()
            print(my_list)
        }
        