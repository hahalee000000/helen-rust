import std.core.*
import std.str.*
import std.list.*
import std.dict.*
import std.math.*
import std.debug.*

        main {
            let my_list = [1, 2, 3]
            let popped = my_list.pop()
            print(popped)
            print(my_list)
        }
        