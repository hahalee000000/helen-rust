import std.core.*
import std.str.*
import std.list.*
import std.dict.*
import std.math.*
import std.debug.*

        main {
            let my_list = [1, 2, 3]
            let copy = my_list.copy()
            copy.append(4)
            print(my_list)
            print(copy)
        }
        