import std.core.*
import std.str.*
import std.list.*
import std.dict.*
import std.math.*
import std.debug.*

agent Worker() {
    main {
        return llm act "what was the secret code?"
    }
}
main {
    print(Worker())
}
