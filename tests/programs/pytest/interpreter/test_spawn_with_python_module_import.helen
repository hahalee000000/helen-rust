import std.core.*
import std.str.*
import std.list.*
import std.dict.*
import std.math.*
import std.debug.*

import "math" as Math

agent Worker(reply: Channel) {
    main {
        reply.send({"status": "ok"})
        reply.close()
    }
}

main {
    let mb = spawn Worker()
    let r = mb.receive()
    print(r["status"])
}
